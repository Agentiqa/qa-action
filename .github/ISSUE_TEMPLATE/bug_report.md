---
name: Bug report
about: Something in the Action misbehaved (wrong gating, missing summary, upload failure)
labels: bug
---

**What happened**

**Expected behavior**

**Details**
- Action version (`uses:` ref):
- `cli-resolved-version` output (or the `agentiqa vX.Y.Z` line in the step summary):
- `fail-on` setting and the run's `outcome` output:
- Runner OS:

**Step summary / logs**
Paste the relevant part of the job step summary and the `Gate on outcome` step log.
If plans behaved unexpectedly (rather than the Action itself), include the `runUrl` from the summary so we can inspect the run.
